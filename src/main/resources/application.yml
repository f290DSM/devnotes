spring:
  application:
    name: devnotes
  datasource:
    url: jdbc:h2:mem:fatec
    username: aluno
    password: password
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true
  h2:
    console.enabled: true
